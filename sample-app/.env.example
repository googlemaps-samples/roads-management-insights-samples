# Application Mode Configuration
# Controls whether the application runs in demo or production mode
# Values:
#   - DEMO: Uses static data files from the /data directory for demonstration purposes
#   - PRODUCTION (or any other value): Connects to BigQuery and fetches live data
# Default: demo (if not specified)
# APPLICATION_MODE=demo

# Google API Key for accessing Google services
# GOOGLE_API_KEY=YOUR_GOOGLE_API_KEY

# --- CITY CONFIGURATION --- 
# This file stores configuration variables for different cities.
# Each city can have its own BigQuery project and dataset for historical data and routes.
# Install the 'python-dotenv' package to load these settings into your app:
# pip install python-dotenv

# --- CITY CONFIGURATION --- 
# Replace `CITY_NAME` with the actual city name (in uppercase) where you are setting up the environment.

# Google Cloud Project ID for the specified city
# CITY_NAME_BIGQUERY_PROJECT=your-google-cloud-project-id
# Example: "my-gcp-project" - This is the Google Cloud project where your BigQuery data is stored.

# BigQuery dataset name for historical data
# CITY_NAME_BIGQUERY_HISTORICAL_DATASET=your-historical-dataset-name
# Example: "traffic_data" - The dataset in BigQuery that holds the city's historical data (traffic, weather, etc.).

# BigQuery table name for historical data
# CITY_NAME_BIGQUERY_HISTORICAL_TABLE=your-historical-table-name
# Example: "historical_traffic" - The specific table in BigQuery that contains the city's historical data.

# BigQuery table name for routes data
# CITY_NAME_BIGQUERY_ROUTES_TABLE=your-routes-table-name
# Example: "routes" - The table in BigQuery that holds route information for the city.

# Timezone of the specified city
# CITY_NAME_TIMEZONE=your-city-timezone
# Example: "Europe/Paris" - The timezone of the city (helps with time-based data accuracy).

# List of use cases for the specified city
# CITY_NAME_USECASES=realtime-monitoring,data-analytics
# Example: ["realtime-monitoring", "data-analytics"] - The use cases for the city's data, such as:
# - "realtime-monitoring": To track live traffic conditions.